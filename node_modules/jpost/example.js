
var jpost = require('./')
var http = require('http')

http.createServer(function(req, res) {
  req.pipe(process.stdout)
  res.end('thnx\n')
}).listen(3500)

jpost('localhost:3500', { some: 'data' }, function(err, res) {
  if (err) throw err
  res.pipe(process.stdout)
})


